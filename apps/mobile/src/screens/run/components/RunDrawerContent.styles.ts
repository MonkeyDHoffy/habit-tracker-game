import { StyleSheet } from "react-native";
import { spacing } from "../../../theme/spacing";

export const runDrawerContentStyles = StyleSheet.create({
  textBlock: {
    gap: spacing.sm,
  },
  menuList: {
    marginTop: spacing.sm,
    gap: spacing.sm,
  },
});
